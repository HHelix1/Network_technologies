# Берем готовый образ с Python 3.8 как основу
FROM python:3.8

# Говорим: "Работать будем в папке /usr/src/app внутри контейнера"
WORKDIR /usr/src/app

# Копируем ВСЕ файлы из текущей папки на компьютере в контейнер
COPY . .

# Когда контейнер запустится, выполни эту команду:
CMD ["python", "hello.py"]
